# The Architect

Design the systems intelligence operates within.

## Run locally
```bash
npm install
npm run dev
```

Then open http://localhost:3000

## Pages
- `/` — Home
- `/protocol` — The Architect Protocol
- `/roles/system-support` — Public canonical role example
